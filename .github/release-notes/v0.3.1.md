# Dream SVNManager v0.3.1

## 重点更新

- 修复历史 Diff Preview 文本工具栏文字不可见的问题。
- 新增 `update.json` 更新清单，支持 stable / beta 通道。
- 更新包下载后会校验 SHA256，校验失败不会安装。
- 发布脚本会自动生成 Release zip、SHA256 和更新清单。

## 发布说明

- 默认通道为 stable。
- 少量试用人员可以在“更多 -> 设置”里切到 beta 通道。
- 如果 `update.json` 暂时不可用，客户端仍会回退检查 GitHub 最新 Release。
